# Future Home of FastAPI app

This is where we'd put a little sign that says "coming soon" and maybe some instructions for how to run the API locally for testing. For now, the API code lives in `src/api/` but it's not fully set up yet.
