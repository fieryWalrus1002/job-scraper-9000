# Diagrams

This directory contains the generated dependency diagrams for the project. The diagrams are generated using the `gen-deps.sh` script, which can be run from the root of the project.

```bash
  bash scripts/gen-deps.sh              # → diagrams/
  bash scripts/gen-deps.sh /other/path  # → custom dir
```
