.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
.env

# scraped data — keep the directory, ignore the files
data/raw/*.jsonl
# ignore any stray jsonl files at the root (e.g. from simple-scrape.py)
/*.jsonl

# local stuff I don't want to commit
.deprecated/
.notes/

# known failures don't need to be tracked in git
config/known_failures.json